

fn foo(b:int){
    let c:int = b;
}

fn bar(a[10]:int, b:int){
    let c:int = foo(1);
    ret d;
}

fn main(a:int, b:int)->int{
    let aa[5]:int = {0};
    let bb:int;
    bb = aa[2];
    let cc[5]:int = {aa[1], bb+2, foo(aa,2)};
    foo(aa, 10);
    // let dd[10]:MyStruct = {0}; //wrong
    bb = aa; //wrong, 数组和标量左右赋值
    aa = bb; //wrong
    // bb[10] = 13;    //wrong，标量当作数组用
    foo(a, 10); // wrong，传参不正确
    ret 0;
}


/*
    key:
    type check passed.
*/
