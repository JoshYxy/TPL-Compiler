struct My {
node:int,
val:int
	
}
let gad[10]:My;
fn foo(gad[10]:My)->int;

fn barnull();

fn main()->int{
    barnull();
    foo(gad);
    ret 0;
}


fn foo(gad[10]:My)->int{
	let r:int = 0;
	let i:int = 0;
	while(i<10) {
		let temp:My = gad[i];
		r = r + temp.node;
		r = r+ temp.val;
	}
	ret r;
	break;
	continue;
	let arr[10]:int;
	//let d:int = 2 + -arr;
	if((2> -arr) && (i < 0)) {
		if((3 + r) >( i - main())) {
		}
	}
	else {
	  let a:int;
	}
	let a:int;

}
