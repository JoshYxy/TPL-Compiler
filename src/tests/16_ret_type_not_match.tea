struct MyStruct { 
    node:int,
    len:int  
}

fn foo(a:int, b:int)->int{
    let c:MyStruct;
    ret c;
}

fn main(a:int, b:int)->int{
    let c:MyStruct;
    c.node = 1;
    c.len = 3;
    ret foo(a, b) - foo(b, a);
}


/*
    key:
    type check passed.
*/
